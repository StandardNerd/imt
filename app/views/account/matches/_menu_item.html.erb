<% if can? :read, Match.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :matches]),
    label: t('matches.navigation.label'),
  } do |p| %>
    <% p.icon do %>
      <i class="<%= t('matches.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
